<?xml version="1.0" ?>
<ruleset
    name="Custom Ruleset"
    xmlns="http://pmd.sf.net/ruleset/1.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd"
>
    <!-- Exclude stubs, which are empty classes and functions defined for PHPStan -->
    <exclude-pattern>stubs/*</exclude-pattern>

    <!-- Exclude unit test files -->
    <exclude-pattern>tests/*</exclude-pattern>

    <!-- Exclude other directories -->
    <exclude-pattern>bower_components/*</exclude-pattern>
    <exclude-pattern>node_modules/*</exclude-pattern>
    <exclude-pattern>var/*</exclude-pattern>
    <exclude-pattern>vendor/*</exclude-pattern>

    <rule ref="rulesets/cleancode.xml">
        <!-- Avoiding else expresssions does not help -->
        <exclude name="ElseExpression" />

        <!-- Calling static functions is necessary sometimes -->
        <exclude name="StaticAccess" />
    </rule>
    <rule ref="rulesets/controversial.xml" />
    <rule ref="rulesets/design.xml" />
    <rule ref="rulesets/naming.xml">
        <!-- Customized below -->
        <exclude name="LongVariable" />
        <exclude name="ShortVariable" />
    </rule>
    <rule ref="rulesets/unusedcode.xml" />

    <!-- 99% of our PHP variable names are shorter than 20 characters so allow 25 -->
    <rule ref="rulesets/naming.xml/LongVariable">
        <properties>
            <property name="maximum" value="25" />
        </properties>
    </rule>

    <rule ref="rulesets/naming.xml/ShortVariable">
        <properties>
            <property name="exceptions" value="db,id" />
        </properties>
    </rule>
</ruleset>
