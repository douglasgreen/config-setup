#!/usr/bin/env bash

# script/setup: Set up application to initial state.

set -e

cd "$(dirname "$0")/.."

script/bootstrap

# Set up DB if the script exists
if [ -f "script/setup_db.sh" ]; then
    echo "==> Setting up DB..."
    script/setup_db.sh
else
    echo "==> No script/setup_db.sh found, skipping DB setup."
fi

# Set up environment variables if the .env file exists
if [ -f ".env" ]; then
    echo "==> Setting up environment variables..."
    source .env
else
    echo "==> No .env file found, skipping environment setup."
fi

echo "==> Repo is now ready to go!"
